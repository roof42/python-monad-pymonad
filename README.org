# Install virtualenv
pip install virtualenv

# Create a virtual environment
virtualenv venv

# Activate the virtual environment
source venv/bin/activate

# To install dependencies from requirements.txt elsewhere
pip install -r requirements.txt
